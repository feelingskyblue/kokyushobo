<ul>
  {%- for article in collections.article -%}
    {%- if article.url != "/blog/" -%}
      <li><a href="{{ article.url }}">{{ article.data.title }}</a></li>
    {%- endif -%}
  {%- endfor -%}
</ul>